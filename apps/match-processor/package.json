{
  "name": "match-processor",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "package": "cp lambda-package.json dist/package.json && cd dist && zip -r ../function.zip .",
    "deploy:local": "aws --endpoint-url=http://localhost:4566 lambda create-function --function-name match-processor --runtime nodejs18.x --handler index.handler --zip-file fileb://function.zip --role arn:aws:iam::000000000000:role/lambda-role || aws --endpoint-url=http://localhost:4566 lambda update-function-code --function-name match-processor --zip-file fileb://function.zip",
    "test:local": "aws --endpoint-url=http://localhost:4566 lambda invoke --function-name match-processor --payload '{}' output.json",
    "clean": "rm -rf dist function.zip && aws --endpoint-url=http://localhost:4566 lambda delete-function --function-name match-processor || true",
    "dev": "npm run clean && npm run build && npm run package && npm run deploy:local && npm run test:local",
    "logs": "aws --endpoint-url=http://localhost:4566 logs filter-log-events --log-group-name '/aws/lambda/match-processor' --start-time $(($(date +%s%N)/1000000 - 300000)) --query 'events[*].message' --output text"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.848.0",
    "@aws-sdk/lib-dynamodb": "^3.848.0",
    "aws-lambda": "^1.0.7",
    "zod": "^4.0.5"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "@types/node": "^24.0.15",
    "esbuild": "^0.25.8",
    "serverless-localstack": "^1.3.1",
    "typescript": "^5.8.3"
  }
}
